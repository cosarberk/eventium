apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eventium.fullname" . }}
  labels:
    {{- include "eventium.labels" . | nindent 4 }}
spec:
  # Container başlangıçta migration çalıştırır; çok replika yarışır. Ölçeklemeden
  # önce migration'ı ayrı Job'a al ve config.RUN_MIGRATIONS=false yap.
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "eventium.selectorLabels" . | nindent 6 }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        {{- include "eventium.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: app
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: eventium
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
            - name: api
              containerPort: 4000
          envFrom:
            - configMapRef:
                name: {{ include "eventium.fullname" . }}
            - secretRef:
                name: {{ include "eventium.fullname" . }}
          startupProbe:
            httpGet:
              path: /health/live
              port: 4000
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            periodSeconds: 10
            timeoutSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
