/**
 * @fileoverview Render primitives, re-exported from `@eventium/shared`.
 *
 * These are consumed by component renderers after the design layer resolves
 * bindings. The single source of truth lives in the shared package.
 */

export type {
  PanelBadge,
  PanelCell,
  PanelColumn,
  PanelSeries,
  PanelSeriesPoint,
  PanelSeverity,
  PanelStat,
  PanelTimelineItem,
} from '@eventium/shared';
